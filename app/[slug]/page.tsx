import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { keywordPageMap, keywordPages } from "@/src/content/keyword-pages";
type PageProps = { params: Promise<{ slug: string }> };
export function generateStaticParams() { return keywordPages.map((page) => ({ slug: page.slug })); }
export async function generateMetadata({ params }: PageProps): Promise<Metadata> { const { slug } = await params; const page = keywordPageMap.get(slug); if (!page) return {}; return { title: page.title, description: page.description, alternates: { canonical: page.path }, openGraph: { title: page.title, description: page.description, url: page.path, type: "article" } }; }
export default async function KeywordPage({ params }: PageProps) { const { slug } = await params; const page = keywordPageMap.get(slug); if (!page) notFound(); const related = keywordPages.filter((item) => item.category === page.category && item.slug !== page.slug).slice(0, 4); return <main className="pt-24 pb-20"><section className="border-b border-white/8"><div className="mx-auto grid max-w-7xl gap-10 px-4 pb-16 sm:px-6 lg:grid-cols-[0.82fr_0.48fr] lg:px-8"><div><div className="mb-5 inline-flex rounded-full border border-emerald-300/20 bg-emerald-300/10 px-3 py-1 text-sm font-medium text-emerald-100">{page.category}</div><h1 className="max-w-4xl text-4xl font-extrabold leading-tight text-white md:text-6xl">{page.keyword}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-stone-300">{page.heroLead}</p><div className="mt-8 rounded-2xl border border-amber-300/15 bg-amber-300/8 p-5"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-amber-200">Quick answer</p><p className="mt-3 text-base leading-7 text-stone-200">{page.quickAnswer}</p></div></div><aside className="rounded-2xl border border-white/10 bg-[#0a1512] p-6 shadow-2xl shadow-emerald-950/20"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-emerald-100">Practical checklist</p><ul className="mt-5 space-y-3">{page.checklist.map((item) => <li key={item} className="flex gap-3 text-sm text-stone-300"><span className="mt-0.5 flex h-5 w-5 flex-shrink-0 items-center justify-center rounded-full bg-emerald-300/20 text-xs font-bold text-emerald-100">+</span>{item}</li>)}</ul><Link href="/pricing" className="mt-7 inline-flex w-full items-center justify-center rounded-xl bg-emerald-200 px-5 py-3 text-sm font-bold text-slate-950 hover:bg-amber-200">Review a contract</Link></aside></div></section><section className="mx-auto max-w-4xl px-4 py-16 sm:px-6 lg:px-8"><div className="space-y-12">{page.sections.map((section) => <article key={section.heading}><h2 className="text-2xl font-bold text-white">{section.heading}</h2><p className="mt-4 text-base leading-8 text-stone-400">{section.body}</p><ul className="mt-5 space-y-3">{section.bullets.map((bullet) => <li key={bullet} className="flex gap-3 text-stone-300"><span className="mt-2 h-2 w-2 flex-shrink-0 rounded-full bg-amber-300" /><span className="leading-7">{bullet}</span></li>)}</ul></article>)}</div><div className="mt-14 rounded-2xl border border-emerald-300/20 bg-gradient-to-br from-emerald-400/12 via-stone-800/40 to-[#0a1512] p-8"><h2 className="text-2xl font-bold text-white">Turn the guide into a contract review summary</h2><p className="mt-3 max-w-2xl text-stone-300">Upload a PDF or Word agreement, review source-linked risk flags, and export a summary you can use before negotiating or escalating to counsel.</p><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Link href="/pricing" className="rounded-xl bg-emerald-200 px-5 py-3 text-center text-sm font-bold text-slate-950 hover:bg-amber-200">Start with Pro</Link><Link href="/legal-contract-review-example" className="rounded-xl border border-white/15 px-5 py-3 text-center text-sm font-semibold text-stone-200 hover:border-emerald-200/50 hover:bg-white/5">See review example</Link></div></div></section><section className="border-t border-white/8 py-16"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><h2 className="text-2xl font-bold text-white">Related guides</h2><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{related.map((item) => <Link key={item.slug} href={item.path} className="rounded-2xl border border-white/10 bg-white/[0.03] p-5 hover:border-emerald-300/35 hover:bg-white/[0.05]"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-amber-200">{item.intent}</p><h3 className="mt-3 text-base font-semibold leading-6 text-white">{item.keyword}</h3><p className="mt-2 line-clamp-3 text-sm leading-6 text-stone-400">{item.description}</p></Link>)}</div></div></section></main>; }
